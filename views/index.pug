extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/" method="post")
    input(type="url" name="inputlink" required)
    button(type="submit") submit

  p New Link:&nbsp;
    span #{newLink}
